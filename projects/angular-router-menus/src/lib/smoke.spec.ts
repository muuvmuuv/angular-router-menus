import { describe, expect, it } from "vitest"

// Placeholder smoke test so the Vitest "test" job has something to run.
// Replace with real unit tests as coverage grows.
describe("smoke", () => {
	it("runs the test suite", () => {
		expect(true).toBe(true)
	})
})
